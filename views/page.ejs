<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - DCOUTSIDE</title>
    <link rel="stylesheet" href="../style.css">
</head>
<body style="background-color: white;">
    <header>
        <strong class="header-name">logined as: <%= name %></strong>
        <img src="../dc.png" alt="DC Logo" width="300" height="150">
        <h1>DCOUTSIDE</h1>
        <section class="header-actions">
            <a href="/" class="back-button">홈</a>
        </section>
    </header>

    <main>
        <section class="post-detail">
            <h2><%= title %></h2>
            <p><strong>작성자:</strong> <%= author %></p>
            <p><strong>작성일:</strong> <%= time %></p>
            <p><strong>글 ID:</strong> <%= postId %></p>
            <div class="post-content">
                <p><%= content %></p>
            </div>

            <% if (author === name) { %> <!-- 로그인한 사용자와 글 작성자가 같으면 삭제 버튼 보이기 -->
                <form action="/delete?id=<%= postId %>" method="get">
                    <button type="submit" class="delete-button">글 삭제</button>
                </form>
            <% } %>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 copybara DCOUTSIDE</p>
    </footer>
</body>
</html>
